- model_class = Cursoacademico
.page-header
  %h1=t '.title', :default => model_class.model_name.human

%p
  %strong= model_class.human_attribute_name(:name) + ':'
  %br
  = @cursoacademico.name

.form-actions
  = link_to t('.back', :default => t("helpers.links.back")), cursoacademicos_path, :class => 'btn'
  = link_to t('.edit', :default => t("helpers.links.edit")), edit_cursoacademico_path(@cursoacademico), :class => 'btn'
  = link_to t('.destroy', :default => t("helpers.links.destroy")), cursoacademico_path(@cursoacademico), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
